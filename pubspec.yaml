name: sports_zone
description: sports app using flutter

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=2.19.5 <3.0.0'

dependencies:
  flutter:
   
    sdk: flutter

  cupertino_icons: ^1.0.2
  flutter_svg: ^0.22.0
  google_fonts: ^2.1.0
  blurrycontainer: 
  bloc: ^8.1.2
  http:
  flutter_bloc:
  cached_network_image: 
  flutter_screenutil: ^5.7.0
  

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0


flutter:
 
 

  uses-material-design: true
  assets:
    - assets/icons/
    - assets/images/pngwing.com.png


